// copy this line and change it to your controller name
// it will than automatically be loaded in the runtime
// which is called in the main.ts.
// That should be all to register a new Controller for
// your project.
export { AppController } from './AppController';